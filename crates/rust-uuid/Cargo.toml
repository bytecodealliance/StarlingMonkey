[package]
name = "rust-uuid"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
uuid = { version = "1", default-features = false, features = ["v4"] }

[profile.release]
lto = true
panic = 'abort'

[profile.dev]
lto = true
panic = 'abort'
